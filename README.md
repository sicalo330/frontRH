# Compilación y ejecución del proyecto

Para compilar el proyecto de recursos humanos, primero debes ubicarte en la carpeta raíz del proyecto `frontRH` y ejecutar el siguiente comando:

```bash
npm install
```
![Texto alternativo](/img/image.png)

#Cuando todo esté instalado usar el comando ng serve o en su forma corta ng s

